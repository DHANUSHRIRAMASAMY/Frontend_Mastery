let name = "Dhanushri"; // String
let age = 20;     // Number
let student = true;   // Boolean
let city;    // Undefined
let data = 0;  // Null

console.log(name + "\n" + age + "\n" + student + "\n" + city + "\n" + data);